Bare minimum set up on a new computer.

1. SymLink the dotfiles 
```
ln -s ~/Dotfiles/.gitconfig ~/.gitconfig
ln -s ~/Dotfiles/.tmux.conf ~/.tmux.conf
ln -s ~/Dotfiles/init.lua ~/.config/nvim/init.lua
```
2. Install neovim
